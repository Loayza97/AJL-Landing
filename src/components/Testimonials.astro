---
// ─── Sección 6: Testimonios en video ────────────────────────────────────────
// Muro de clips verticales reales. Click-to-play + carga diferida:
// al inicio la página solo baja las miniaturas (~100KB), no los videos.
import { testimonios } from '../data/testimonios.js';
---

<section id="section-testimonials" class="section bg-gray">
  <div class="container">
    <p class="label">Pacientes reales</p>
    <h2 class="title" style="margin-bottom: 0.75rem;">
      No te lo contamos nosotros.<br />Te lo cuentan ellos.
    </h2>
    <p class="subtitle" style="margin-bottom: 3rem;">
      Historias reales de personas que cambiaron su relación con la comida —
      parte de los <strong>+800 pacientes</strong> que ya pasaron por el método.
    </p>

    <div class="vt-grid">
      {testimonios.map(t => (
        <figure class="vt-card">
          <div class="vt-video">
            <video
              class="vt-player"
              preload="none"
              playsinline
              poster={t.poster}
              data-name={t.name}
            >
              <source src={t.src} type="video/mp4" />
            </video>
            <button class="vt-play" type="button" aria-label={`Reproducir el testimonio de ${t.name}`}>
              <span class="vt-play__icon" aria-hidden="true">▶</span>
            </button>
          </div>
          <figcaption class="vt-cap">
            <span class="vt-name">{t.name}</span>
            <span class="vt-role">{t.role}</span>
          </figcaption>
        </figure>
      ))}
    </div>

    <p class="t-disclaimer">
      Testimonios reales grabados por pacientes. Los resultados dependen de la
      constancia y el contexto de cada persona.
    </p>
  </div>
</section>

<style>
.bg-gray { background: var(--gray-light); }

.vt-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.25rem;
}

.vt-card {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.vt-video {
  position: relative;
  aspect-ratio: 9 / 16;
  border-radius: 12px;
  overflow: hidden;
  background: var(--black);
  box-shadow: 0 4px 20px rgba(0,0,0,0.12);
}

.vt-player {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* Botón play sobre el poster */
.vt-play {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(180deg, rgba(0,0,0,0.05), rgba(0,0,0,0.35));
  border: none;
  cursor: pointer;
  transition: background var(--transition);
}

.vt-play:hover { background: linear-gradient(180deg, rgba(0,0,0,0.1), rgba(0,0,0,0.45)); }
.vt-play.is-hidden { display: none; }

.vt-play__icon {
  width: 60px;
  height: 60px;
  border-radius: 50%;
  background: var(--gold);
  color: var(--black);
  font-size: 1.4rem;
  display: flex;
  align-items: center;
  justify-content: center;
  padding-left: 4px;
  box-shadow: 0 4px 16px rgba(0,0,0,0.3);
  transition: transform var(--transition);
}

.vt-play:hover .vt-play__icon { transform: scale(1.08); }

.vt-cap {
  display: flex;
  flex-direction: column;
  text-align: center;
}

.vt-name { font-size: 0.95rem; font-weight: 800; color: var(--black); letter-spacing: -0.2px; }
.vt-role { font-size: 0.75rem; color: var(--gray-mid); margin-top: 0.1rem; }

.t-disclaimer {
  font-size: 0.75rem;
  color: #aaa;
  text-align: center;
  margin-top: 2.5rem;
}

@media (max-width: 900px) { .vt-grid { grid-template-columns: repeat(2, 1fr); } }
@media (max-width: 460px) { .vt-grid { grid-template-columns: 1fr; max-width: 320px; margin-inline: auto; } }
</style>

<script>
  // Click-to-play: carga el video solo al pulsar; pausa los demás.
  document.querySelectorAll('.vt-card').forEach(card => {
    const video = card.querySelector('.vt-player');
    const btn = card.querySelector('.vt-play');
    if (!video || !btn) return;

    btn.addEventListener('click', () => {
      document.querySelectorAll('.vt-player').forEach(v => {
        if (v !== video) { v.pause(); }
      });
      video.setAttribute('controls', '');
      video.play();
      btn.classList.add('is-hidden');

      if (typeof window.gtag === 'function') {
        window.gtag('event', 'testimonio_play', { name: video.dataset.name });
      }
    });

    // Al terminar, vuelve a mostrar el poster + botón
    video.addEventListener('ended', () => {
      video.removeAttribute('controls');
      video.currentTime = 0;
      btn.classList.remove('is-hidden');
    });
  });
</script>
